# Configuration de l'API
# URL de l'API backend (en dev: utilise le proxy Vite /api, en prod: URL complete)
VITE_API_URL=/api

# Fonctionnalites optionnelles
# Active les rappels de rendez-vous cote front-end (desactive par defaut, gere par cron cote serveur)
VITE_ENABLE_FRONT_REMINDERS=false

# Mode de notification
# Options: 'mock' (simulation), 'noop' (aucune action), 'api' (appels reels)
VITE_NOTIF_MODE=mock
